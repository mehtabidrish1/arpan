enum RequestType {
  get,
  getWithToken,
  post,
  put,
  postWithToken,
  postWithHeaders,
  postWithRefreshToken,
  delete,
}

enum UserStatus {
  loggedIn,
  loggedOf,
  isFirstLogin,
}
